# Node A configuration
server_host     = 127.0.0.1
server_port     = 8080
# Sensor sampling frequency in Hz (emulated)
sensor_freq_hz  = 50.0
# Output log file for received magnitude results
log_file        = accel_module.log
# Seconds to wait before reconnecting after disconnect
reconnect_delay_s = 3
